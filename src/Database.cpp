#include "Database.hpp"



template<typename T>
vector<shared_ptr<T>> Database<T>::pull(string table) {
    vector<shared_ptr<T>> data{};


    return data;
}

template<typename T>
void Database<T>::push(string table, vector<shared_ptr<T>> data){
    


}